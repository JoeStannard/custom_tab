module ForemanCustomTab
  module HostsHelper
    def summary_fields(host)
    
    #  fields = []
    #  rows.each do |a, b|
    #    fields += [[_(a), host.send(b)]] if host.send(b).present?
    #  end
    #  fields
    end
  end
end
