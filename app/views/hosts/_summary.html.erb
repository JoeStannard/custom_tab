<table class="<%= table_css_classes %>">
  <thead>
    <tr>
      <th colspan="2"><%= _('Summary') %></th>
    </tr>
  </thead>
  <tbody>
    <% summary_fields(host).each do |property, value| %>
      <tr>
        <td><%= property %></td>
        <td><%= value %></td>
      </tr>
    <% end %>
  </tbody>
</table>
